{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "cmake",
            "type": "shell",
            "command": "cmake --build \\\"${workspaceFolder}/build\\\" --config Debug --target all -- -j 4",
            "options": {
                "cwd": "build"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            }
        },
    ]
}